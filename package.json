{
  "name": "vibe-meter",
  "version": "0.1.0",
  "description": "Monitor live Claude Code and Codex sessions — TUI dashboard, tmux integration, and usage tracking.",
  "type": "module",
  "bin": {
    "vibe-meter": "./src/index.js"
  },
  "scripts": {
    "test": "node --test",
    "snapshot": "node ./src/index.js snapshot --json",
    "tui": "node ./src/index.js tui",
    "tmux-status": "node ./src/index.js tmux-status"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT",
  "dependencies": {
    "@clack/prompts": "^1.1.0"
  }
}
